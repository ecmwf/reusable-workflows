# Build type -> GitHub runner labels. Read by lib/cd_config.load_runners (generate_matrix).
default:
  - self-hosted
  - platform-builder
hpc:
  - self-hosted
  - linux
  - hpc
system-package:
  - self-hosted
  - platform-builder-docker-xl
